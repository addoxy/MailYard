'use client';

import { Button } from '@/components/ui/button';
import { Code } from 'lucide-react';

export const ExportEmail = () => {
  return (
    <Button>
      <Code />
      Export
    </Button>
  );
};
